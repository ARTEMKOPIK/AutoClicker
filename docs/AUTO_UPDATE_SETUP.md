# Настройка автообновления приложения

## Как это работает

1. Ты пушишь код с тегом версии (`git tag v1.0.1`)
2. GitHub Actions автоматически собирает APK
3. Создаётся Release с APK файлом
4. Приложение проверяет GitHub API и показывает диалог обновления

## Настройка

### 1. Укажи свой репозиторий

Открой файл `app/src/main/java/com/autoclicker/app/update/UpdateManager.kt` и замени:

```kotlin
private const val GITHUB_OWNER = "YOUR_GITHUB_USERNAME"  // твой username на GitHub
private const val GITHUB_REPO = "YOUR_REPO_NAME"         // название репозитория
```

Например:
```kotlin
private const val GITHUB_OWNER = "artemkopik"
private const val GITHUB_REPO = "AutoClicker"
```

### 2. Подпиши APK (опционально, но рекомендуется)

Для production релизов нужно подписать APK. Добавь в `.github/workflows/release.yml`:

```yaml
- name: Sign APK
  uses: r0adkll/sign-android-release@v1
  with:
    releaseDirectory: app/build/outputs/apk/release
    signingKeyBase64: ${{ secrets.SIGNING_KEY }}
    alias: ${{ secrets.KEY_ALIAS }}
    keyStorePassword: ${{ secrets.KEY_STORE_PASSWORD }}
    keyPassword: ${{ secrets.KEY_PASSWORD }}
```

И добавь секреты в GitHub:
- `SIGNING_KEY` - base64 твоего keystore файла
- `KEY_ALIAS` - alias ключа
- `KEY_STORE_PASSWORD` - пароль keystore
- `KEY_PASSWORD` - пароль ключа

### 3. Как выпустить обновление

```bash
# 1. Обнови CHANGELOG.md с описанием изменений

# 2. Создай тег с версией
git tag v1.0.1

# 3. Запушь тег
git push origin v1.0.1
```

GitHub Actions автоматически:
- Соберёт APK
- Создаст Release
- Прикрепит APK к релизу
- Добавит changelog из CHANGELOG.md

### 4. Формат CHANGELOG.md

```markdown
## [1.0.1] - 2024-01-15

### ✨ Новое
- Добавлена функция X
- Добавлена функция Y

### 🔧 Улучшения
- Улучшена производительность

### 🐛 Исправления
- Исправлен баг Z
```

Эти изменения будут красиво отображаться в диалоге обновления!

## Как работает проверка обновлений

- При запуске приложения автоматически проверяется наличие обновлений
- Проверка происходит не чаще раза в час
- Пользователь может пропустить версию
- В настройках есть кнопка ручной проверки

## Требования

- Репозиторий должен быть публичным (или нужен токен для приватного)
- В релизе должен быть APK файл
- Версия в теге должна быть больше текущей версии приложения
