# AutoClicker

Android приложение для автоматизации действий на экране с поддержкой скриптов.

## Возможности

- 🖱️ Автоматические клики по координатам
- 📝 Скриптовый язык для автоматизации
- 🔍 OCR распознавание текста (ML Kit)
- 🎨 Пипетка для определения цвета пикселя
- 📱 Плавающая панель управления
- 📨 Отправка уведомлений в Telegram
- 💾 Автосохранение скриптов
- 🎯 Подсветка синтаксиса

## Функции скриптов

### Базовые команды

```javascript
// Клик по координатам
click(x, y)

// Долгий клик
longClick(x, y)

// Свайп
swipe(x1, y1, x2, y2)

// Задержка в миллисекундах
sleep(1000)

// Получить текст из области (OCR)
val text = getText(x1, y1, x2, y2)

// Получить цвет пикселя
val color = getColor(x, y)

// Вывод в лог
log("Сообщение")

// Отправка в Telegram
sendTelegram("Сообщение")

// Копировать в буфер обмена
pushToCb("текст")

// Остановить скрипт
EXIT = true
```

### 🤖 AI-Powered команды (NEW!)

```javascript
// Найти текст на экране (возвращает координаты)
val pos = findText("Кнопка")
if (pos != null) {
    click(pos.first, pos.second)
}

// Найти текст с таймаутом
val pos = findText("Загрузка", 10000) // ждать до 10 секунд

// Найти изображение на экране
val pos = findImage("/sdcard/template.png")
if (pos != null) {
    click(pos.first, pos.second)
}

// Найти изображение с порогом совпадения
val pos = findImage("/sdcard/button.png", 0.9) // 90% совпадение

// Ожидать появления текста
if (waitForText(0, 0, 1080, 1920, "Готово", 5000)) {
    log("Текст появился!")
}
```

## Примеры скриптов

### Базовый пример

```javascript
log("Скрипт запущен")

while (!EXIT) {
    // Клик по кнопке
    click(540, 1200)
    sleep(500)
    
    // Проверка цвета
    val color = getColor(100, 100)
    if (color == -1) {
        log("Цвет изменился!")
        sendTelegram("Обнаружено изменение")
    }
    
    sleep(1000)
}

log("Скрипт остановлен")
```

### 🤖 AI-пример: Автоматический поиск и клик

```javascript
log("🤖 AI-скрипт запущен")

// Ищем кнопку "Начать" на экране
val startButton = findText("Начать")
if (startButton != null) {
    log("✅ Кнопка найдена!")
    click(startButton.first, startButton.second)
    sleep(1000)
} else {
    log("❌ Кнопка не найдена")
    EXIT = true
}

// Ждём появления текста "Готово"
if (waitForText(0, 0, 1080, 1920, "Готово", 30000)) {
    log("✅ Процесс завершён!")
    sendTelegram("🎉 Задача выполнена!")
} else {
    log("⏱️ Таймаут ожидания")
}

log("Скрипт завершён")
```

### 🖼️ Пример с поиском изображений

```javascript
log("🖼️ Поиск изображения...")

// Сохраните эталонное изображение кнопки в /sdcard/button_template.png
val buttonPos = findImage("/sdcard/button_template.png", 0.85)

if (buttonPos != null) {
    log("🎯 Кнопка найдена на экране!")
    click(buttonPos.first, buttonPos.second)
    sleep(500)
} else {
    log("🔍 Кнопка не найдена, пробуем текстовый поиск...")
    val textPos = findText("ОК")
    if (textPos != null) {
        click(textPos.first, textPos.second)
    }
}
```

## Требуемые разрешения

- **Accessibility Service** - для выполнения кликов
- **Overlay** - для плавающей панели
- **Screen Capture** - для скриншотов и OCR

## Сборка

```bash
./gradlew assembleDebug
```

APK будет в `app/build/outputs/apk/debug/`

## GitHub Actions

Проект настроен для автоматической сборки APK при push в main/master.
